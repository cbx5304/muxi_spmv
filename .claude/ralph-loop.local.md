---
active: true
iteration: 16
session_id: 
max_iterations: 20
completion_promise: null
started_at: "2026-04-03T01:39:49Z"
---

在两台远程服务器上完成以下工作：
1.继续穷尽一切手段分析X201为什么性能上不去，并尝试各种优化方案进行优化（包括但不限于算法层面、kernel级硬件优化、解析汇编命令并优化等），以获取avgNnz<10的最佳性能。并形成完整的分析报告和优化对比报告。
注意事项：
1.远程服务器（chenbinxiangc@172.16.45.81，ssh端口19936，已配置好免密码）必须在/c/Users/Lenovo/cbx/spmv_muxi/下进行工作，严禁在其他目录写入或修改文件。且仅能使用这台服务器上的GPU7进行所有GPU计算、测试和其他任务。
2.远程服务器（test@172.16.45.70 ssh端口3000，已配置好免密码）必须在/c/Users/Lenovo/cbx/spmv_muxi/下进行工作，严禁在其他目录写入或修改文件。
3.远程服务器（chenbinxiangc@172.16.45.81，ssh端口19936，已配置好免密码）和远程服务器（test@172.16.45.70 ssh端口3000，已配置好免密码）需要安装任何第三方库，均优先采用本地编译安装方式，且仅安装在用户目录，不允许安到系统目录！
4.每个任务完成后，需要沉淀这次任务的经验和遇到的问题及解决方案(含远程服务器上的任务)到本地doc/下的合适目录，以后每次执行任务都从这个目录加载需要的信息。把相关需要注意的、后续任务可能需要知道的（把后面项目执行人当作一个新接手项目的人）、可以减少试错成本的信息 加入到记忆或者形成文档。
5.在本地始终同步一份主分支最新代码。
6.所有编译运行测试工作全部在远程服务器（chenbinxiangc@172.16.45.81，ssh端口19936，已配置好免密码）和远程服务器（test@172.16.45.70 ssh端口3000，已配置好免密码）进行
7.不同功能开发需求需要在不同git分支进行，待功能开发完成并通过测试后才可合并到主分支，完成功能开发后及时清理无用分支。
8.不要把项目中的.claude文件夹放到git中。
9.远程服务器（chenbinxiangc@172.16.45.81，ssh端口19936，已配置好免密码）上的国产GPU支持CUDA11.6及以下的CUDA语法，代码应按CUDA格式进行编写（严禁使用国产GPU的接口函数，避免破坏代码对英伟达显卡的兼容性）。使用CUDA编写的程序在该平台编译时，'cmake xxx'命令全部改成'pre_make cmake xxx'，'make xxx'命令改成'pre_make make xxx'，同时把'~/cu-bridge'下（含子目录）的bin、lib、lib64等目录加入PATH、LIBRARY_PATH等对应环境变量，其会自动检测CUDA相关编译环境并进行自动替换为国产GPU自身对应库，无需在cmakelists中单独设置hc和htcc相关操作，同时不要在cmakelist强制指定CUDA相关的库路径，最好通过find让其自动查找，否则国产GPU无法自动替换对应库。国产GPU不支持打印算力版本号（即CUDA的sm_xx）和CUDA版本号。
10.如果需要对国产GPU进行debug调试，不要使用printf（国产GPU的printf有问题）。可以使用远程服务器（chenbinxiangc@172.16.45.81，ssh端口19936，已配置好免密码）上/c/Users/Lenovo/cbx/muxi_print_bug的日志库
11.远程服务器（chenbinxiangc@172.16.45.81，ssh端口19936，已配置好免密码）的国产GPU，单次warp size是64，和英伟达的32不一致，可在代码中做区分。注意warp size的不同大小会导致单个SM寄存器、共享内存等的使用和英伟达显卡编译出来的程序存在数量差异。需要采用的性能调优方案就不一致。
12.远程服务器（chenbinxiangc@172.16.45.81，ssh端口19936，已配置好免密码）的国产GPU可以使用ht-smi查看GPU使用，类似nvdia-smi。
13.如果需要使用python，要构建虚拟环境（并将该环境的构建编写成脚本，便于后续复现），不允许直接使用系统的python。
14.针对国产GPU和4090的性能开发差异，需要单独在doc/下新建一个目录进行记录，特别是二者为达到相同性能的开发差异和注意点。
